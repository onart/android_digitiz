<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Must descend from Theme.AppCompat: GameActivity extends
         AppCompatActivity, which refuses to start under a platform theme. -->
    <style name="Theme.Digitiz" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>
